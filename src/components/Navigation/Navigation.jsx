import React from "react";

const Navigation = () => {
  return (
    <nav
      style={{ display: "flex", justifyContent: "flex-end", padding: "1rem" }}
    >
      <p className="f3 link dim white shadow underline pa3 pointer">Sign Out</p>
    </nav>
  );
};

export default Navigation;
